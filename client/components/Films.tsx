import Image from "next/image";

const Films = () => {
  return (
    <section className="w-full h-screen bg-slate-600 relative" id="films">
      {/*   <Image src={cover} alt="cover" className="w-full h-screen object-cover" /> */}
    </section>
  );
};

export default Films;
